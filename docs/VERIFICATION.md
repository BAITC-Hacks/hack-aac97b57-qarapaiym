# Проверка официального кейса — 2026-09-23

Эта запись заменяет проверку прежней модели с бюджетом1 000. Источник требований: полный текст кейса, присланный командой, транскрипция в `OFFICIAL_CASE.md`. Исходный коммит4aa80e4 не соответствовал его датасету, формуле и правилам выбора. Проверяется исправленное рабочее дерево; изменения ещё не опубликованы.

## Подтверждённые исправления

|ID|Важность|Первоначальное нарушение|Текущее доказательство|
|---|---|---|---|
|F01|CRITICAL|Бюджет1 000, другие данные и15мер|100, официальные50значений/доли,14мер M1–M14|
|F02|CRITICAL|Среднее пяти категорий вместо Score|Независимый расчёт базы52.55768 и примера56.54307 совпал с движком|
|F03|HIGH|По одной мере на категорию, без выбора района|Пять уникальных, максимум2/сферу; официальные2социальные меры в Нуре принимаются|
|F04|HIGH|Нет лагов/синергий/конфликтов|Тесты всех трёх синергий/конфликтов, горизонт8, clamp после суммы|
|F05|HIGH|У невалидного сценария был projected Score|Теперь projected/delta null; стоимость101 отклонена|
|F06|HIGH|AI объяснял другую модель|Канонический серверный расчёт,10метрик,таргеты,вклады,синергии в запросе|
|F07|HIGH|Документы и демо описывали старый кейс|README/CASE/контракт/демо/AT-02 обновлены по официальному источнику|
|F08|HIGH|AI25с/2400токенов не завершал официальный пример|Провайдер ответил за31с со статусом incomplete/max_output_tokens; лимит60с/4000 и краткий формат; A HTTP200 за14.847с, B HTTP200 за12.236с|
|F09|MEDIUM|Проверка evidence не доказывает достоверность прозы|Структурно корректная противоречивая фраза проходит; README раскрывает ограничение|
|F10|MEDIUM|Turbopack EPERM в этой среде|Документированный webpack production build проходит|

## Независимые числовые сценарии

A: M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка.
B: тот же набор, M7 перенесён в Есиль.
E: M1 Нура,M2 город,M4 Сарыарка,M5 Сарыарка,M8 Нура.
O: M1 Нура,M2 город,M5 Сарыарка,M6 город,M14 город.

|Сценарий|Расход|Остаток|Score|Критических|Статус|
|---|---:|---:|---:|---:|---|
|База|0|100|52.55768 →52.56|2|PASS|
|A|95|5|56.54307 →56.54|0|PASS|
|B|95|5|55.29777 →55.30|1|PASS|
|E|100|0|55.33933 →55.34|1|PASS|
|O|101|−1|Не считается|null|PASS: отказ|

Для A районные баллы до округления: Есиль63.4275,Алматы57.4975,Сарыарка56.30,Байконур57.0675,Нура52.9625. Средневзвешенный58.0776. Синергия M10/M12 добавила B1+2 в Нуре. Прирост округляется от неокруглённых Score:3.98539 →3.99.

## Тесты и сборка

- PASS:22/22 текущих теста (13 движка,9 AI/API).
- PASS:TypeScript `tsc --noEmit` и production TypeScript-проверка.
- PASS:production `next build --webpack` в отдельной копии; маршруты `/` и `/api/analyze`.
- FAIL в этом хосте:обычный Turbopack build ранее повторно завершался EPERM; fallback указан в README.
- PASS:независимый свежий reviewer проверил все120перестановок официального примера, exact100/101,повторы,неверные районы,городскую меру с районом,max2,три конфликта,три синергии,clamp и строгий порог40.

## Acceptance status

|Требование|Статус|Доказательство|
|---|---|---|
|AT-01 одинаковый старт|PASS|Импорт фиксированногоJSON, база52.56/100 в браузере|
|AT-02 пять решений|PASS|Официальный пример с2социальными; валидатор5уникальных/max2|
|AT-03 бюджет|PASS|100 принят;101отвергнут движком и API-тестом|
|AT-04 детерминизм|PASS|120перестановок, повтор и неизменность исходных данных|
|AT-05 изменение результата|PASS для контролируемого A/B|56.54 против55.30; разные планы могут иметь равные агрегаты|
|AT-06 Score считает код|PASS|Общая функция движка, каноническая серверная проверка|
|AT-07 реальныйAI|PASS наличия/связи с результатом; точность прозы имеет дефектMEDIUM|Живые A/B HTTP200; в одном A S2 Нуры указан43 вместо43.75; таблицы правильны|
|AT-08 плохойAI-ответ|PASS|Тесты отказа/неполного/пустого/слишком большогоJSON|
|AT-09 ключ/провайдер|PASS по тестам и наблюдённому timeout|Числа сохраняются; ошибки без секрета|
|AT-10 чистый запуск|PASS|Новая копия без зависимостей/секретов: npm ci,test,typecheck,webpack build,start HTTP200; no-key API503|
|AT-11 build|PASS webpack / FAIL default на этом хосте|Рабочий документированный fallback|
|AT-12 A/B|PASS|Браузер56.54→55.30, сравнение−1.24, расход95/95|
|AT-13 официальный набор/база|PASS|Независимые проверки50значений,14мер и формулы|
|AT-14 эффекты|PASS|Все3синергии/конфликта,лаги,clamp,штрафы|
|AT-15 invalid/exact/order|PASS|projected:null для невалидного;100/101;120перестановок|

## Границы выводов

Подставной ответ теста не является доказательством реального AI. Свободная проза не имеет строгой проверки каждого утверждения. Разные планы не обязаны иметь уникальный скалярный Score: равные взвешенные суммы и округление могут дать совпадение. Тест изменения решений проверяет содержательный A/B, не искусственно меняет формулу ради уникальности.

Общий сервер3000 не перезапускался; исправленная версия проверяется отдельным production-сервером4328. Для проверки текущего результата используйте именно его либо сборку рабочего дерева.

## Итоговый браузерный проход

PASS: база100/52.56; 0,1,4 решения не готовы; официальныйA95/5→56.54; изменение M7 Нура→Есиль очищает прежний результат; B95/5→55.30 с живым AI; сравнение B−A−1.24. Точный бюджет100 принят интерфейсом и даёт55.34. План85 с четырьмя мерами блокирует M14 ценой16; удаление M6 снова делает M14 доступной. Сброс возвращает100/0из5, старый незавершённый ответ не возвращает результаты. Production API отверг стоимость101 с HTTP400 INVALID_SCENARIO.

PASS: свежая установка текущего рабочего дерева в отдельную папку (без node_modules, .next и секретов): npm ci79пакетов,22теста,typecheck,webpack build,production HTTP200. API без ключа503 AI_NOT_CONFIGURED.

## Независимый повторный контроль

Fresh A: официальный кейс/датасет/формула PASS; найдено HIGH-расхождение старых документов, исправлено.
Fresh B: движок/бюджет PASS, включая независимую арифметику и120перестановок.
Fresh C: живые A/B PASS по доступности и контексту; MEDIUM фактическая ошибка свободной прозы (35→43 вместо35→43.75), LOW неподтверждённое слово «косвенно» о влиянии на транспорт при нулевых T1/T2.
Reviewer D: создание четвёртого нового агента отклонено лимитом среды; исходный независимый judge повторил проверку с нуля на новой копии, PASS воспроизводимости. Это не четыре новых контекста; ограничение явно раскрыто.

Оставшиеся проблемы: MEDIUM точность отдельных AI-утверждений; MEDIUM несовместимость обычной Turbopack-сборки с данным хостом при рабочем webpack fallback. Подтверждённых CRITICAL/HIGH дефектов текущего расчёта или основного сценария после исправлений нет.

## Строгие пять официальных критериев

1. PASS: одинаковый бюджет100 и исходные данные.
2. PASS: exact100 принят,101отвергнут интерфейсом/движком/API.
3. PASS: решения/районы меняют официальные показатели.
4. FAIL по фактической точности AI: реальные ответы и содержательные компромиссы есть, но наблюдалось неверное35→43 вместо35→43.75. Успешный HTTP200 не равен полностью корректному анализу.
5. PASS для контролируемогоA/B:56.54→55.30.

Итог: ALIGNED WITH MATERIAL GAPS. Основной поток, движок, бюджет и локальная воспроизводимость проходят. Строгий общий case-fit и точность AI нельзя объявить PASS до устранения подтверждённой ошибки свободного текста. Её важностьMEDIUM: она не меняет детерминированный Score или таблицы; по разрешённому объёму исправлялись только CRITICAL/HIGH. Опубликованный коммит4aa80e4 по-прежнему содержит прежнюю модель: этот отчёт относится к локальному исправленному дереву, не к неизменённому GitHub.
